import SwiftUI

struct SubscriptionsView: View {
    var body: some View {
        VStack {
            Text("Subscriptions")
            // TODO: Add subscription list and feed URL input.
        }
    }
}

struct SubscriptionsView_Previews: PreviewProvider {
    static var previews: some View {
        SubscriptionsView()
    }
}